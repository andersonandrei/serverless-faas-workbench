apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: rnngenerator
  namespace: knative-functions
spec:
  template:
    spec:
      containers:
      - name: rnngenerator
        image: docker.io/andersonandrei/functionbench-knative:rnngenerator
        volumeMounts:
        - name: functionbench
          mountPath: /data
        resources:  
          requests:
            cpu: 1
            memory: 1024M
          limits:
            cpu: 1
            memory: 1024M               
      volumes:
      - name: functionbench
        persistentVolumeClaim:
          claimName: functionbench
          readOnly: false
      serviceAccountName: knative-rbac          