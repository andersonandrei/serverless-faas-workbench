apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: linpack
  namespace: knative-functions
spec:
  template:
    spec:
      containers:
      - name: linpack
        image: docker.io/andersonandrei/functionbench-knative:linpack
        resources:  
          requests:
            cpu: 1
            memory: 1024M
          limits:
            cpu: 1
            memory: 1024M 